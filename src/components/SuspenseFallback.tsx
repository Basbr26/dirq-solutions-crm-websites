// Minimal fallback for Suspense during route transitions
// No loading animation shown for instant page switches

export const SuspenseFallback = () => {
  return null; // Instant, no loading shown
};

export default SuspenseFallback;
